# ChatBox 开发记录

> 本文档记录**实施过程**：时间线、踩的坑、验证结果、版本历史。
> 想了解"这个插件现在是怎么工作的"，请看 [`技术方案.md`](技术方案.md)。
>
> 分工原则：**技术方案.md 永远只描述现状**（不留"待确认"），
> 过程性的、会过时的东西全部放这里。

---

## 版本历史

| 版本 | DLL SHA256 前 16 位 | 主要变化 |
|---|---|---|
| 0.1-capture | `1EA6B69BE42D6B01` | 阶段 1：只捕获消息 + 落盘，不做任何显示 |
| 0.2-window | `EC0297A633159EBE` | 阶段 2 首版：消息框显示 —— **实测崩溃** |
| 0.2.1-window | `765E6E5BC4C84ACD` | 修调用约定（`FillRectTrans` / 文本绘制） |
| 〃 | `D5CD4A8FD2F47CD9` | 修裁剪高度（字只显示上半截） |
| 〃 | `0710C6DCEF48DA0E` | 修 `pBounds` 语义（第 2/3 行被裁）；改为新消息在下 |
| 〃 | `AF7F0F5FD34169A6` | 加自动折行；`MaxLines` 语义改为"行数" |
| 0.3 | `84F4A053D4C12ACC` | 阶段 3a：展开/收起（Ctrl+M）+ 键盘滚动；修右侧边距不对称 |
| 〃 | `1A67697232B9CD93` | 修行号方向（**一个符号错误造成三个症状**）+ 展开态忽略超时 |
| 〃 | `25412B46F8FDE55F` | 修滚动量积压（夹紧后未写回状态） |
| 〃 | `77B249F137277C77` | 阶段 3b：鼠标点击 / 滚轮 / 吞掉穿透点击（3 个新钩子） |
| 〃 | `1197EDFECF3CEA6B` | 修滚轮方向判据（bit31 而非 bit15） |
| 〃 | `D56B5135E208B2CB` | 加滚动条（贴底板右侧外侧） |
| 〃 | `B2903A89A645DAFC` | 加"向上翻页时冻结自动滚动" |
| 〃 | `A725C3AADEA65F4C` | 系统消息改用当前玩家颜色（探针实测 `ColorSchemeIndex`） |
| 〃 | `7C9E67122890B2E1` | 颜色：改用 `0x517440` 换算显示色（**走错路**） |
| 〃 | `63781A51944597E1` | 颜色：改内联汇编传参 + 加诊断日志（**仍是错路**） |
| **〃** | **`AEF18BAAD848CAF6`** | **颜色：改用引擎的 ColorScheme 版本绘制（正解）** |
| **0.3.1** | **`EB2CD90039ED8441`** | 调整默认值：`LogRaw=0`、`ExpandLines=15`、`SysUseMyColor=0`（并升版本号） |
| **0.3.2** | **`C0CE91AA6007E6A8`** | `LogRaw` 改为"调试字段总开关"；默认日志只留人能看的内容 |
| **0.3.3** | **`5D0167E2A5774E41`** | `draw:` / `ui:` 两类界面诊断行也收进 `LogRaw` |
| 〃 | `132BEF2FF5EEAE3C` | 修 `g_Compose` 共享缓冲（玩家聊天会全部显示同一条）；加 `scroll:` 滚动诊断 |
| 〃 | `D5EE5A956232B5DE` | 翻页步长改为"跟随可见行数"（**随后被用户否决**） |
| **0.3.6** | **`5F8B4534D25ABA8A`** | 翻页步长定为固定 5 行，并做成配置项 `ScrollStep` |
| **0.3.7** | **`184B41CF8E76731D`** | **修联机发现的输入框消失**：绘制钩点从 `0x4F455D` 改为 `0x5D4A94` |
| **0.3.8** | **`34BAECBB5AD7D874`** | 新增 `TopGapLines`：顶部按行预留空档，避免盖住回车输入框 |

> 0.2.1 连续修了四次，都是同一类问题的不同侧面：**对引擎绘制 API 的理解**
> （调用约定、裁剪语义）。详见下面的踩坑记录。

---

## 时间线

### 前置调研：读 Phobos 的 MessageColumnClass

Phobos 有一个功能相近的实现 `MessageColumnClass`（`Misc/MessageColumn.h/.cpp`，
192 + 1116 行），调研结论：

- 它寄生在原版 `MessageListClass` 之上，用一堆 `GadgetClass` 派生控件
  （切换按钮、上下翻页、滚动条、标签）实现交互
- 绘制全走引擎的 `DSurface` / `BitFont`，**不碰 DirectX**
- 记录存在 `ScenarioExt::RecordMessages`（`std::vector<std::wstring>`），
  随存档序列化
- **但它只在一个调用点捕获消息**（`0x6DE122`，`TActionClass::Execute` 内部），
  所以只能抓到触发器文本

**结论：思路可借鉴，但不能直接用** —— Phobos 依赖 Ares + 完整框架，
而目标环境是纯原版。ChatBox 用纯 C++ 受限子集自己实现。

### 阶段 1：只捕获，不显示

**目标**：验证"钩函数入口能抓到全部消息"，并找出快捷键提示的特征。

做法：钩 `MessageListClass::AddMessage` 的函数入口，把 7 个参数 + 调用者
返回地址全部写进日志。

**为什么先做这一步**：消息分类是后面所有设计的前提（哪些该显示、哪些该过滤、
颜色从哪来），而这些只能靠实测数据定，不能拍脑袋。

**用户跑一局后的数据**（MO 战役，56 条消息）：

| 返回地址 | 次数 | 类型 | cs | to | silent |
|---|---|---|---|---|---|
| `0x00732BF5` | 21 | 快捷键提示 | 7 | 240 | **1** |
| `0x006DE127` | 12 | 剧情/任务文本 | 28 | 1259 | 0 |
| `0x00730AC8` | 12 | 路径点模式提示 | 7 | 480 | **1** |
| `0x004F9E01` | 4 | 结盟消息 | 6 | 1259 | 0 |
| `0x0055F0FA` | 3 | 玩家聊天（本地） | 7 | 1259 | 0 |
| `0x004C79F4` | 1 | 系统消息 | 15 | 1259 | 0 |
| `0x0048D97E` | 1 | 玩家聊天（远程） | 15 | 1259 | 0 |
| `0x00430D31/6E` | 2 | 通信信标提示 | 7 | 225 | 0 |

**两个关键结论**：

1. **`silent` 参数（YRpp 里误标为 `SinglePlayer`）就是操作提示的标志** ——
   所有快捷/路径点提示都是 1，所有真消息都是 0，零例外
2. **`ColorSchemeIdx` 就是发言者颜色** —— 聊天消息的 `cs` 与其后系统消息
   （"玩家X改变了游戏速度"）的 `cs` 一致

### 阶段 2：消息框显示

接管绘制 + 半透明底板 + 按玩家颜色着色 + 过滤操作提示。

**绘制接管方案**：钩 `MessageListClass::Draw` **内部**"画消息列表"的那三条指令
（`0x5D4A94`），接管时返回 `0x5D4A9B`。这样原版的消息链表、超时消失、提示音
全都不受影响，只是不出图；**同一个函数里的输入框仍由原版绘制**。

> 初版钩在更外层的调用点（`0x4F455D`）整个跳过 `Draw`，后来联机时发现
> 输入框也一起没了 —— 详见踩坑记录第 12 条。

> 对比 Phobos：它让消息**不进**原版链表，因此必须自己补提示音。

### 阶段 2b：折行

用引擎自己的折行函数 `0x433F50`（原版 `Manage` 用的同一个）算"给定像素宽度下
能放几个字符"，长消息按此切行。`MaxLines` 语义从"条数"改为"行数"，
避免一条超长消息撑满屏幕。

### 阶段 3a：展开 / 收起 + 键盘滚动

**先做键盘版**（`Ctrl+M` 切换、方向键/翻页键滚动，折叠 8 行 / 展开 30 行）。
理由："展开看历史"这个核心价值用键盘就能拿到，而鼠标有一个必须小心的风险
—— **点击会穿透到战场**。分两步走，每步都能独立验证。

### 阶段 3b：鼠标交互

三个新钩子，共同保证"点消息框不会穿透到战场/侧栏"：

| 地址 | 作用 |
|---|---|
| `0x777998` | 读滚轮方向 → 自己滚动 |
| `0x533F50` | 悬停消息框时跳过侧栏滚动 |
| `0x692419` | 命中消息框时跳过点击坐标处理（**吞掉穿透点击**） |

同一轮里还补了**滚动条**（贴底板右侧外侧，不占文字宽度）、
**向上翻页冻结自动滚动**、以及**按消息来源着色**。

---

## 踩坑记录

### 1. ⚠️ 崩溃：调用约定错了（`__stdcall` vs `__thiscall`）

**现象**：进游戏后立刻崩溃。

```
Exception code: C0000005 at 004BB83D
ECX: 00000001     ← 垃圾值
syringe.log: "The process tried to read from 0x00000001"
```

**根因**：`Surface::FillRectTrans` 是**成员函数**，`this` 必须在 ECX 里。
我写成了 `__stdcall`，把 `this` 当**栈参数**传 —— ECX 保持了寄存器里的垃圾值 1。

```
4bb837:  mov %ecx,%edi     ; edi = this = 1
4bb83d:  mov (%edi),%eax   ; 读 0x00000001 → 崩
```

**定位方法**：崩溃地址 → objdump 反汇编 → 对照寄存器值。
`0x4BB830` 的函数结尾 `ret $0xC`（3 个栈参数）与
`FillRectTrans(RectangleStruct*, ColorStruct*, int)` 完全吻合，
说明**虚表索引没错，只是传参方式错了**。

**修正**：用 `__fastcall` 表达 thiscall（前两个参数走 ECX/EDX，其余走栈，
且被调方清栈）。

**顺带发现第二个同类问题**：`Simple_Text_Print_Wide @ 0x4A5EB0`
**也不是 stdcall** —— 反汇编 `0x4A5EC2` 有 `mov %ecx,0x14(%esp)`（它读 ECX），
结尾 `ret $0x1C`（7 个栈参数）+ ECX/EDX = 9 个，与 YRpp 声明完全一致。
**YRpp 里标的 `JMP_STD` 具有误导性。**

> **经验**：判断一个引擎函数的调用约定，看它的 `ret N` 清理多少字节 ——
> 那等于栈参数个数。再结合入口是否读 ECX/EDX 就能确定寄存器参数。

### 2. 字只显示上半截

**现象**：消息框能画出来，但每个字只有上半截。

**根因**：`pBounds->Height` 是**裁剪高度**，我直接传了字高 `lineH`。

**修正**：给到两倍行高。

### 3. 只有第一行显示完整，第二行半截，第三行起空白

**现象**：修复 #2 之后，第一行正常，第二行只有半截，第三行及以后完全空白。

**根因**：`pBounds` 的语义理解错了。它**既是坐标原点又是裁剪区**：

```
实际绘制位置 = pBounds 原点 + pLocation
裁剪范围     = (pBounds.X, pBounds.Y, pBounds.X+Width, pBounds.Y+Height)
```

我把 `pBounds` 固定在 `(0,0)`、高度只给 `2×lineH = 34`，再用 `pLocation`
把文字送到框外：

| 行 | 文字 y 范围 | 裁剪区 [0,34] | 现象 |
|---|---|---|---|
| 第1行 | [8, 25] | 界内 | 完整 |
| 第2行 | [25, 42] | 超出 34 | 半截 |
| 第3行起 | [42, …] | 完全在外 | 空白 |

**修正**：`pBounds` 传**整个消息框**，`pLocation` 只表示框内偏移。

> **经验**：反汇编 `0x4A5F96~0x4A5FAB` 的几条 `add` 在算
> `X+Width` / `Y+Height`（右/下边界）—— 这就是裁剪区的证据。

### 4. 空日志文件

**现象**：一次启动游戏出现两份日志，其中一份只有头部没有内容。

**根因**：游戏启动器进程也会加载本 DLL，但它一条消息都不产生。

**修正**：日志改为**惰性创建** —— 只在捕获到第一条消息时才建文件写头部。

### 5. `%TEMP%` 每次调用都在变

**现象**：编译脚本依赖 `%TEMP%\chatbox_build`，但下次调用时该目录不存在。

**根因**：DSH 沙箱给每次工具调用分配带随机后缀的临时目录
（`C:\Users\...\Temp\dsh-XXXXXX\`）。另外沙箱**不允许**写
`%LOCALAPPDATA%\Temp` 本身（只允许 `dsh-*` 目录）。

**处置**：编译命令改为"**按需复制工具链**"（目标已存在就跳过）。
`build.bat` 本来就没这个问题 —— 它每次都自己 staging。

### 6. 编译产物哈希不稳定

**现象**：同一份源码连续编译两次，哈希不同，文档里记的哈希无法复现。

**排查**：固定输出文件名连编两次 → 哈希一致；换输出名 → 差 341 字节
（偏移 `0xB6`/`0xD8`/`0x4B3`…，即 PE 头与嵌入的路径字符串）。

**结论**：两个变量 —— **PE 链接时间戳**（可禁）与**输出路径**（只能靠固定构建目录）。
`build.bat` 已加 `-Wl,--no-insert-timestamp`。

### 7. `ColorScheme::BaseColor` 的偏移是推断的

**背景**：YRpp 里没有该字段的 `static_assert`。我先从
`unknown_314[0x1C]`（**名字里自带偏移**的字段）反推出 `ShadeCount=0x310`、
`LightConvert=0x30C`、`BaseColor=0x308`、`ID=0x304`，
而 `Colors`（`BytePalette`，768 字节）从 `0x004` 起正好到 `0x304` —— 完全自洽。

**但自洽不等于正确**，所以后用探针 `probe/probe_color.cpp` 实测确认：

```
offsetof(ColorScheme, BaseColor) = 0x308   ✓ 与推断一致
sizeof(ColorStruct)              = 3，R@+0 G@+1 B@+2
```

> **经验**：凡是"推导出来"的偏移，都在实机/探针上钉一次再写进文档。

### 8. ⚠️ 行号方向反了 —— 一个符号错误造成三个症状

**现象**（用户反馈）：
1. 新消息跑到最上面
2. 折叠态总显示**最旧**的几条，新消息永远看不到
3. 翻页时底板像"拉窗帘"一样伸缩

**根因**：`g_Picked[0]` 是**最新**的一条，而我分配行号时从 0 往上加 ——
最新的拿到行号 0。但"最新在最下面"要求它拿**最大**行号。方向正好相反。

| 现象 | 成因 |
|---|---|
| ① 新消息在上面 | 行号小的画在上面，最新恰好行号最小 |
| ② 折叠态显示最旧 | "取最新 8 行"= `[totalAll-8, totalAll)`，在反向行号下是最旧的 8 行 |
| ③ 翻页像拉窗帘 | 显示区间与滚动方向相反 |

**修正**：从 `totalAll` 往回减，让 `picked[0]` 拿最大行号。

> **经验**：三个看起来不同的症状可能只是同一个方向性错误的三种投影。
> 报症状时给"具体表现"比给"我觉得哪里错了"更有用 —— 这次正是靠
> "第一行正常、第二行半截、第三行空"这类精确描述定位的。

### 9. ⚠️ 状态夹紧未写回导致"反向要按同样多次数"

**现象**：`PageUp` 到顶后多按几次，再按 `PageDown` 必须按**同样多次数**才生效。

**根因**：`g_Scroll` 上限只在绘制时夹紧，**夹紧后的值没有写回**：

```
上限 50，连按 10 次 PageUp → g_Scroll=80（积压 30）
按 PageDown → g_Scroll=72，仍被夹在 50 → 画面不动
按 4 次（减 32）→ g_Scroll=48 → 才开始动
```

`PageDown` 方向没这问题（它的下限夹紧写在按键处理里），所以只坏一边。

**修正**：夹紧后把值写回状态本身。

> **经验**：带边界的累加量，**夹紧之后一定要落回状态本身**，不能只在消费端夹紧。
> 否则"按过头"的量会静默积压。

### 10. ⚠️ 滚轮方向判据写错了位

**现象**：滚轮向上正常，向下完全没反应。

**根因**：Phobos 用 `WParam & 0x80000000u`（**bit31**），我写成 `& 0x8000u`（bit15）。
`0x777998` 处那条 `shr $0x10,%ecx` **正是被钩子覆盖掉的原指令** —— 所以钩子
进入时 ECX 还是原始 WParam，bit15 恒为 0，于是**无论往哪滚都走"向上"分支**。

> **经验**：**参考实现里已经写对的判据，照着用，不要自己重新推导。**
> 这是同一个毛病在本项目里的第二次发作（见第 11 条）。

### 11. ⚠️⚠️ 颜色问题的三重弯路（本项目最大的教训）

**现象**（用户三次反馈，一次比一次离谱）：

| 反馈 | 我的做法 |
|---|---|
| 橙黄→土黄、淡蓝→黄、黄→淡蓝 | 直接读 `ColorScheme::BaseColor` |
| 橙黄→紫、淡蓝→红、黄→粉紫 | 改调 `0x517440` 做"显示色换算" |
| （仍不对） | 改内联汇编传参 + 加诊断日志 |

**诊断日志给出了关键数据**：

```
cs=32  base=(40,255,255)  shown=(255,240,0)
        ↑青色的 BaseColor      ↑正好是原版的黄色
```

这证明**我的取色其实是对的**，问题在"把颜色值交给引擎"这一步 —— 于是范围
一下子收窄了。

**真正的根因**：原版画消息文本用的是 **`Fancy_Text_Print_Wide` 的 ColorScheme
重载（`0x4A61C0`）**，传的是 **`ColorScheme*` 指针**，根本不是 RGB 值：

```
5d4a4f:  mov  (%eax,%edx,4),%ecx   ; ecx = ColorScheme*
5d4a56:  push %ecx                 ; ★ 传指针
5d4a79:  call 0x4a61c0
```

我一直在走 `0x4A5EB0`（接受"颜色 dword"）那条路，**而那条路的颜色语义既不是
RGB 也不是 COLORREF** —— 所以怎么算都对不上。

**修正**：改用 `0x4A61C0` + 传方案指针，颜色换算全交给引擎。副作用是
**不再需要 `0x517440`**，那一整类问题都消失了。

> **经验（最重要的一条）**：
> **在"自己构造一个引擎已经封装好的调用"之前，先去读原版对应的实现。**
> 这三次弯路的共同点都是：我拿着一个地址就开始拼参数，而没有先问
> "原版自己是**怎么调**这个功能的"。原版 `MessageListClass::Draw` 就在手边
> （`0x5D49A0`，我在阶段 1 就反汇编过它），里面清清楚楚写着正确做法。
>
> 相关的两个具体技巧：
> - **看 `ret N` / `add $N,%esp` 判断栈参数个数与清栈方**（`add $0x20,%esp`
>   说明 8 个参数、调用方清栈 ⇒ cdecl）
> - **诊断日志要打印"输入 vs 输出"**，这样能一刀切开"算错了"和"传错了"

### 12. ⚠️ 联机才暴露的输入框消失（钩子范围过大）

**现象**（联机测试反馈）：按回车输入消息时，左上角**看不到"从【玩家名】："输入框**了。
但实际能打字、回车能发出、发出的消息也能看到。

**根因**：`MessageListClass::Draw`（`0x5D49A0`）**同时画两样东西**，而我钩在更外层的
调用点（`0x4F455D`）把它**整个跳过**了：

```
5d49b4:  mov  0x19(%esi),%al   ; IsEdit
5d49b9:  je   0x5d4a8d         ; IsEdit==0 -> 跳过【输入框】部分
5d49bf:  ...                   ; 画输入框 (EditLabel @ this+0x24)
5d4a8d:  mov  (%esi),%ecx      ; this->MessageList
5d4a92:  je   0x5d4a9b
5d4a94:  mov  (%ecx),%edx      ; ★ 这里才画【消息列表】
5d4a98:  call *0x2c(%edx)
```

**修正**：改钩 `0x5D4A94`（覆盖 mov/push/call 共 7 字节），返回 `0x5D4A9B` ——
只跳过消息列表，输入框仍由原版绘制。

**为什么单机测试一直没发现**：单机不便手动打字（用户早先就提到过），
这条路径从未被走到。**是联机测试把它逼出来的。**

> **经验**：
> 1. **"跳过某个函数"之前，先确认它只干你要跳过的那一件事。**
>    我假定 `MessageListClass::Draw` 只画消息列表，实际上它还画输入框 ——
>    而且输入框的绘制被 `IsEdit` 包着，看反汇编开头很容易只注意到消息相关的那段。
> 2. **依赖真实使用路径的功能（输入框、提示音）只有在会走到它的场景下才能测出来。**
>    单机测不到的，联机可能一测就现。

---

## 验证记录

| 项目 | 方法 | 结果 |
|---|---|---|
| DLL 加载 | `test/test_load.c` | `failures: 0` |
| 导出函数齐全 | 同上 | 6 个（5 个钩子 + `SyringeHandshake`） |
| 消息捕获 | 用户实机（MO 战役） | 56 条全部抓到，7 个参数值合理 |
| 快捷键过滤 | 用户实机 | 按快捷键不再刷屏 |
| 消息框显示 | 用户实机 | 多行完整显示，新消息在下 |
| 折行 | 用户实机 | 通过（用 `MaxWidth=200` 制造折行验证） |
| 展开 / 键盘滚动 | 用户实机 | 通过 |
| 鼠标点击 / 滚轮 / 不穿透 | 用户实机 | 通过 |
| 滚动条 | 用户实机 | 通过 |
| 冻结自动滚动 | 用户实机 | 通过 |
| 按来源着色 | 用户实机 | 通过（三种颜色均与原版一致） |
| 崩溃定位链路 | `except.txt` → objdump → 寄存器对照 | 两次调用约定问题均定位成功 |

---

## 当前版本

**ChatBox 0.3.8** —— 捕获、日志、消息框、折行、展开/滚动、鼠标交互、滚动条、
冻结自动滚动、按来源着色均已实机验证。

| 产物 | SHA256 前 16 位 | 大小 |
|---|---|---|
| `ChatBox.dll` | `34BAECBB5AD7D874` | 30,561 字节 |

- 编译：`-Wall` 无警告无错误
- 加载验证：`failures: 0`
- 钩子：**5 个**（`0x5D3BA0` / `0x5D4A94` / `0x777998` / `0x533F50` / `0x692419`）
- 依赖：KERNEL32.dll、USER32.dll
- 与 NoCopyProtect(7) + AutoLoad(1) + AutoKit(1) 共存时
  Syringe 应显示 `Done (15 hooks added)`

---

## 下一步（可选，用户已明确放弃其中几项）

用户评估后**放弃**的：时间戳（日志里有就够）、未读提示（意义不大）、
复制文本（选区交互工作量大）、消息来源单列（日志里有）。

仍然可做的：

- **滚动条拖拽** —— 目前滚动条只是指示，不支持鼠标拖动
- **无日志版构建** —— 目前只有带日志版（AutoKit 有双版本先例）
- **多存档/多环境适配** —— 目前日志按"进程启动时间"分文件
