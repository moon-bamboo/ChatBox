# ChatBox 技术方案

> **ChatBox** 是一个 Syringe 插件，为《命令与征服：尤里的复仇》1.001 提供
> **消息历史 + 屏幕消息框**：把所有文本消息记录成日志，并用一个现代聊天框
> 风格的窗口显示，可过滤刷屏的快捷键提示、按玩家着色、支持滚动与展开。
>
> **当前版本 0.4.3** —— 捕获、日志、消息框、折行、展开/滚动、鼠标交互、滚动条、
> 冻结自动滚动、按来源着色、历史条数上限、超长截断断句均已实机验证。
> 开发过程与踩坑记录见 [`开发记录.md`](开发记录.md)。
>
> 与 AutoKit / NoCopyProtect **平级但完全独立**：ChatBox 只读消息、只画 UI、
> 只读输入，不下发任何事件、不碰任何游戏状态。

---

## 0. 定位与设计原则

### 0.1 它和原版消息系统的关系

原版把文本消息统一交给 `MessageListClass`，显示在**左上角**，最多 14 条
（`wchar_t MessageBuffers[14][162]`），每条按调用方给的超时帧数自动消失。

ChatBox **不动原版的消息系统，只在"显示"这一层做文章**：

| 环节 | 是否改动 |
|---|---|
| 消息产生（`AddMessage`） | ❌ 不拦，照常执行 |
| 消息进链表、超时消失 | ❌ 不动 |
| 提示音（"叮"） | ❌ 不动 |
| **画到屏幕上** | ✅ 跳过原版绘制，改画自己的窗口 |
| **额外记录一份** | ✅ 写进内存历史和日志文件 |

好处：**原版行为的副作用一个都不丢**。对比 Phobos 的 `MessageDisplayInCenter`
—— 它让消息**不进**原版链表，因此必须自己补提示音，且原版消息逻辑会失效。

### 0.2 联机安全性（逐项论证）

**结论：只需自己安装，不需要对手安装，也不会造成不同步。**

关键前提：**lockstep 同步的是「指令序列」，而不是"鼠标位置"或"画面内容"。**
插件全程**不下发事件、不修改任何游戏对象**，只做三件事：读内存、读输入、画本地屏幕。

| 钩子 | 做什么 | 触碰同步状态？ |
|---|---|---|
| `0x5D3BA0` 捕获消息 | **只读** 7 个参数与返回地址，返回 0 原样执行 | ❌ 否 |
| `0x55D360` 每帧一次 | 只在**本地**读键盘/鼠标状态（热键、悬停、滚动） | ❌ 否 |
| `0x4F455D` 接管绘制 | 仅跳过**本地**的"画消息框"（输入框保留），返回 0 原样执行 | ❌ 否 |
| `0x5D4A94` 抑制原版列表 | 仅跳过**本地**的原版消息列表绘制 | ❌ 否 |
| `0x777998` 读滚轮 | **只读** ECX（滚轮方向），返回 0 原样执行 | ❌ 否 |
| `0x533F50` 跳过侧栏滚动 | 悬停消息框时不滚侧栏（纯本地 UI 操作） | ❌ 否 |
| `0x692419` 吞掉穿透点击 | 悬停消息框时这次点击不被游戏处理 | ❌ 否（见下） |

> **为什么输入和绘制挂在两个不同的地址**（0.4.3 起）：
> `0x5D4A94` 位于 `MessageListClass::Draw` 内部，原版代码在它前面有一条
> `je` —— **原版消息列表为空时会跳过它**。早期版本把输入处理也放在这里，
> 于是"消息全部消失后 Ctrl+M 失灵、连日志都不写"。
> 现在输入挂在每帧必到的 `0x55D360`，绘制挂在每帧必到的调用点
> `0x4F455D`，而 `0x5D4A94` 只负责"别让原版再画一遍"。

**"吞掉点击"为什么不影响同步**：

它等价于"玩家把鼠标停在消息框上、没有去点战场"。
不吞 → 游戏产生一条指令并广播给所有人；吞掉 → 游戏**不产生**这条指令。
两者都是**合法的本地选择**，与其他玩家完全无关。

而它最坏的表现也只是**本地操作失灵**（万一命中判定出错，吞掉了战场该处理的点击），
**不会**造成不同步 —— 因为插件从不下发事件。

**装与不装、装什么版本，互不影响**：

- 各客户端的消息框内容**可以不同**（显示设置、过滤规则、窗口位置各自独立）
- 这与"分辨率 / 画质设置不同"是同一类事情
- 未安装的一方看原版消息列表，一切照旧
- 联机聊天能正常抓到：日志验证过两种来源 —— `0x0055F0FA`（本地发言）与
  `0x0048D97E`（**远程玩家**发言），对手的消息经网络到达后同样经过 `AddMessage`

**唯一前提**：不能改用 Phobos 那类"钩引擎逻辑"的范式，否则会变成全员必须安装。

### 0.3 与 Ares / Phobos 的兼容性

**Ares：完全兼容。** Ares 没有钩 ChatBox 使用的任何一个地址（已核对 Ares 源码）。
实测环境亦验证：MO（基于 Ares 3.0）下工作正常。

**Phobos：四个地址重叠，但实际可共存。**

| 地址 | ChatBox | Phobos | 结果 |
|---|---|---|---|
| `0x5D3BA0` / `0x5D4A94` | 钩 | — | ✅ |
| **`0x55D360`** | 每帧读输入 | `MainLoop_FrameStep_Begin` | ✅ 两者都 `return 0` → 链式调用（AutoKit 同址共存已被实机验证，此处是三方共存） |
| `0x4F455D` | 画消息框 | — | ✅ |
| `0x4F4589` | — | 画消息栏 | ✅ 同函数内不同地址（相距 `0x2C`，覆盖区间不重叠） |
| **`0x777998`** | 读滚轮 | 读滚轮 | ✅ 两者都 `return 0` → 链式调用 |
| **`0x533F50`** | 条件跳过 | 条件跳过 | ⚠️ 见下 |
| **`0x692419`** | 条件吞点击 | 条件吞点击 | ⚠️ 见下 |

> **地址来源说明**：`0x55D360` 的身份直接取自 Phobos 源码的钩子名
> `MainLoop_FrameStep_Begin`（AutoKit 的 `兼容性说明.md` 已核对 Ares 不钩此地址，
> 且 AutoKit 的自动修理正是靠它工作）—— **不需要自己反汇编去论证**。
> 同理 `0x4F4589` 是 Phobos 画消息栏的地址，它能在那里正常显示，
> 就说明所在函数每帧都会执行。

**Syringe 的链式机制**（源码 `SyringeDebugger.cpp:170` 的 `code_call` 模板）：

```
CALL 我们的钩子
MOV  fs:0x14, EAX        ; 保存返回值
CMP  fs:0x14, 0
JE   proceed             ; 0 → 继续执行同一地址上的下一个钩子
JMP  fs:0x14             ; 非 0 → 立即跳走
```

即 **`return 0` 的钩子会被依次调用（可共存）**；
**`return <非0>` 会立即跳转，短路掉同一地址上后面的钩子**。

**实际影响很小**：ChatBox 与 Phobos 的 UI 区域不重叠（左上角 vs 屏幕中央），
"两者同时要求拦截"基本不会发生 —— 鼠标只可能落在其中一个上，另一个本来也会返回 0。

**唯一需要注意的组合**：Phobos 把 `MessageDisplayInCenter` 设为 `true` 时，
它的 `AddTActionMessage` 会**不再调用原版 `MessageListClass::AddMessage`**：

```cpp
if (Phobos::Config::MessageDisplayInCenter)
    MessageColumnClass::Instance.AddMessage(...);   // ← 绕开了原版入口
else
    pThis->AddMessage(...);                          // ← ChatBox 钩的就是这里
```

**后果**：ChatBox 会**漏掉触发器 / 任务类文本**（那类消息不再经过原版入口）。
玩家聊天不受影响（走别的调用路径）。该选项 Phobos 默认为 `false`，默认配置下无此问题。

---

## 1. 七个钩子

| 地址 | 覆盖 | 作用 |
|---|---|---|
| `0x5D3BA0` | 6 字节 | `MessageListClass::AddMessage` **函数入口** —— 捕获所有消息 |
| `0x55D360` | 5 字节 | 每帧一次 —— 读键盘热键、鼠标悬停、滚轮累计 |
| `0x4F455D` | 5 字节 | 每帧一次 —— 画消息框（**无条件到达**，0.4.3 起） |
| `0x5D4A94` | 7 字节 | 抑制原版"画消息列表"（**链表为空时不经过**） |
| `0x777998` | 6 字节 | 读滚轮方向 → 滚动消息框 |
| `0x533F50` | 5 字节 | 悬停消息框时**跳过侧栏滚动** |
| `0x692419` | 7 字节 | 命中消息框时**跳过点击坐标处理** → 点击不穿透到战场 |

### 1.0 为什么是「输入 / 绘制 / 抑制」三个钩子

0.4.2 及以前只有一个 `0x5D4A94` 同时负责输入与绘制，它有个致命弱点：
**这条路径是可跳过的**。

```
5d4a8d:  mov  (%esi),%ecx      ; this->MessageList
5d4a90:  test %ecx,%ecx
5d4a92:  je   0x5d4a9b         ; ← 链表为空就跳过 5d4a94！
```

原版消息全部超时消失后，链表为空，`0x5D4A94` **再也不被执行** ——
于是 `Ctrl+M` 没反应、连一行日志都不写。而玩家看到的现象是
"消息框没了，然后就彻底死了"，很难联想到"钩子被跳过"。

0.4.3 拆成三处，各自挂在**每帧必经**的位置上：

| 职责 | 地址 | 是否每帧必到 |
|---|---|---|
| 读输入（热键/悬停/滚轮） | `0x55D360` | ✅ |
| 画消息框 | `0x4F455D` | ✅ |
| 阻止原版再画一遍 | `0x5D4A94` | ❌ 只在原版列表非空时 |

这样即便原版链表为空，消息框照画、热键照响应。

### 1.1 捕获：为什么钩「函数入口」

`AddMessage` 是所有文本消息的唯一入口（聊天、触发器文本、快捷键提示、
超武提示、系统消息）。Phobos 只在某一个调用点插桩（`0x6DE122`，
`TActionClass::Execute` 内部），因此**只能抓到触发器文本** —— 实测一局
56 条消息里它那条路只能覆盖 12 条。

钩在**函数入口**时 `sub $0x14c,%esp` 尚未执行，**ESP 正好指向返回地址**，
7 个参数依次在 `ESP+4 .. ESP+1C`：

| 偏移 | 参数 | 说明 |
|---|---|---|
| `+0x00` | 返回地址 | **消息是谁发的**（用于分类） |
| `+0x04` | `const wchar_t* Name` | 来源（仅玩家聊天非空） |
| `+0x08` | `int ID` | 玩家聊天时是发言者槽位 |
| `+0x0C` | `const wchar_t* Message` | 正文 |
| `+0x10` | `int ColorSchemeIdx` | **发言者颜色方案** |
| `+0x14` | `int Style` | TextPrintType |
| `+0x18` | `int Timeout` | 超时帧数，`-1` = 永不消失 |
| `+0x1C` | `int silent` | **见 §2，关键** |

`REGISTERS` 里 `_ESP` 在 `+0x14`、`_ECX` 在 `+0x20`（YRpp/Syringe.h:115，
字段顺序即内存顺序）。

### 1.2 绘制：钩 `Draw` 内部，只跳过"消息列表"

**`MessageListClass::Draw`（`0x5D49A0`）同时负责画两样东西** —— 输入框和消息列表：

```
5d49a0:  sub $0x24,%esp
5d49b4:  mov  0x19(%esi),%al   ; IsEdit (MessageListClass+0x19)
5d49b9:  je   0x5d4a8d         ; IsEdit==0 → 跳过【输入框】部分
5d49bf:  ...                   ; 画输入框 (EditLabel @ this+0x24)
5d4a8d:  mov  (%esi),%ecx      ; this->MessageList (this+0x00)
5d4a90:  test %ecx,%ecx
5d4a92:  je   0x5d4a9b         ; 链表空则跳过
5d4a94:  mov  (%ecx),%edx      ; ★ 这里才画【消息列表】
5d4a96:  push $0x1
5d4a98:  call *0x2c(%edx)      ; MessageList->Draw(1)
5d4a9b:  add  $0x24,%esp
5d4a9e:  ret
```

所以钩在 **`0x5D4A94`**（覆盖 `mov`(2) + `push`(2) + `call`(3) = **7 字节**），
接管时返回 `0x5D4A9B` —— 只跳过"画消息列表"，**输入框仍由原版绘制**。

跳转安全：`pop %esi`（`0x5D4A8F`）在钩点之前已执行、栈已平衡，
而 `0x5D4A9B` 的 `add $0x24,%esp` 正是原版的栈清理。

> ⚠️ **踩过的坑 1**：最初钩在更外层的调用点 `0x4F455D`（原版 `call MessageListClass::Draw`）
> 并整个跳过 —— **联机测试时发现按回车看不到"从【玩家名】："输入框了**。
> 单机测试没暴露，是因为单机不便手动打字、这条路径从未被走到。
>
> ⚠️ **踩过的坑 2**：修好之后，输入处理（`PollKeys`/`PollMouse`）也留在了
> `0x5D4A94` 上 —— 于是**原版链表一空，热键就全部失灵**。
> 0.4.3 把输入搬到 `0x55D360`、把绘制搬到 `0x4F455D`（此处不再返回非 0，
> 而是 `return 0` 让原版 `Draw` 照常执行、由 `0x5D4A94` 负责抑制列表），
> 两者都是**每帧必到**的地址。详见 §1.0。

### 1.3 鼠标三个钩子的配合

点击与滚轮若不拦截会**穿透到战场/侧栏**，所以：

- `0x692419`（`DisplayClass::ProcessClickCoords`）：命中消息框 → 跳 `0x6925FC`
  （DoNothing），这次点击由消息框消费
- `0x533F50`（侧栏滚动）：悬停消息框 → 跳 `0x533FC3`，滚轮只滚消息
- `0x777998`（滚轮 WndProc）：读方向自己滚动

**三者都只在"鼠标落在消息框矩形内"时才生效**，否则行为与原版完全一致。

---

## 2. 消息分类：`silent` 参数

阶段 1 采集 56 条真实消息后的重要发现：`AddMessage` 第 7 个参数在 YRpp 里
叫 `SinglePlayer`，**但它其实是 `silent`**（控制是否播放提示音）。

| 返回地址 | 次数 | 类型 | cs | to | **silent** |
|---|---|---|---|---|---|
| `0x00732BF5` | 21 | **快捷键提示** | 7 | 240 | **1** |
| `0x006DE127` | 12 | 剧情/任务文本 | 28 | 1259 | 0 |
| `0x00730AC8` | 12 | **路径点模式提示** | 7 | 480 | **1** |
| `0x004F9E01` | 4 | 结盟消息 | 6 | 1259 | 0 |
| `0x0055F0FA` | 3 | **玩家聊天**（本地） | 7 | 1259 | 0 |
| `0x004C79F4` | 1 | 系统消息 | 15 | 1259 | 0 |
| `0x0048D97E` | 1 | **玩家聊天**（远程） | 15 | 1259 | 0 |
| `0x00430D31/6E` | 2 | 通信信标提示 | 7 | 225 | 0 |

**规律：操作提示类全是 `silent=1`，真消息全是 `silent=0`，零例外。**

快捷键提示频率极高（一局 21 次），会把消息框完全刷满，所以
**默认 `FilterSilent=1`**：操作提示不进消息框（但**照常写日志**）。

---

## 3. 绘制

### 3.1 用到的引擎 API

| 用途 | 地址 | 约定 |
|---|---|---|
| 画布 | `DSurface::Composite = 0x88731C` | 全局指针 |
| 可视区 | `DSurface::ViewBounds = 0x886FA0` | `RectangleStruct` |
| 字体 | `BitFont::Instance = 0x89C4D0` | 全局指针（`+0x1C` = 字高） |
| **文本绘制** | `0x4A61C0` | **`__cdecl`，8 个栈参数，收 `ColorScheme*`** |
| 折行计算 | `0x433F50` | thiscall（ECX=BitFont），4 个栈参数 |
| 半透明底板 | `Surface::FillRectTrans` | 虚表 **`+0x1C`** |
| 颜色方案数组 | `ColorScheme::Array = 0xB054D0` | `Items@+4`、`Count@+0x10` |
| 当前玩家颜色 | `HouseClass::ColorSchemeIndex = 0x16054` | |
| 鼠标坐标 | `WWMouseClass::Instance = 0x887640`，`XY1@+0x1C` | 游戏绘制坐标系 |

**全程使用引擎自带 API，不碰 DirectX。**

### 3.2 文本绘制必须用 ColorScheme 重载（重要）

`Fancy_Text_Print_Wide` 有**两个重载**：

- `0x4A60E0` / `0x4A5EB0` —— 接受**颜色值**
- **`0x4A61C0` —— 接受 `ColorScheme*` 指针** ← **原版用的就是这个**

反汇编原版 `MessageListClass::Draw` 得到确证：

```
5d4a44:  mov  0x34(%eax),%edx      ; 消息的颜色方案索引
5d4a47:  mov  0xb054d4,%eax        ; Array.Items  (= 0xB054D0+4)
5d4a4f:  mov  (%eax,%edx,4),%ecx   ; ecx = ColorScheme*
5d4a56:  push %ecx                 ; ★ 传的是【指针】
5d4a79:  call 0x4a61c0
5d4a7e:  add  $0x20,%esp           ; 8 个栈参数 ⇒ __cdecl
```

**必须走这条路**：颜色换算（BaseColor → 显示色、明暗档位）全部交给引擎，
我们既不调 `0x517440`、也不自己拼 RGB。曾经自己算 RGB 走了三次弯路，
详见开发记录的"颜色三重弯路"。

### 3.3 `pBounds` / `pLocation` 的语义（搞错三次）

```
实际绘制位置 = pBounds 原点 + pLocation
裁剪范围     = (pBounds.X, pBounds.Y, pBounds.X+Width, pBounds.Y+Height)
```

- **`pBounds` = 整个消息框**（既是坐标原点，又是裁剪区）
- **`pLocation` = 框内相对偏移**

反汇编依据：`0x4A5F96~0x4A5FAB` 的几条 `add` 在算右/下边界。

### 3.4 折行

用引擎自己的折行函数（原版 `Manage` 用的同一个）：

```cpp
int __thiscall 0x433F50(BitFont* this, const wchar_t* text, int width, int a4, int a5)
// 返回"给定像素宽度下能放几个字符"; a4/a5 原版传 111 与 1
```

**硬换行（文本自带的 `\n`）优先断开。**
`MaxLines` 限制的是**行数**而非条数 —— 否则一条超长消息就能撑满屏幕。

### 3.5 滚动条

贴在底板**右边缘外侧** 1 像素处，不占文字宽度（画在框内会导致折行宽度变化、
滚动时整屏文字重排）。

- 滑块高度 = `轨道高 × 可见行 / 总行`
- 位置：`skip=0`（看到最新）在**底部**，因为新消息在下

---

## 4. 内存历史与显示模型

### 4.1 存储

固定大小环形缓冲，**不做动态分配**（无 CRT 环境下自己管内存易错）：

```cpp
#define MAX_MSGS          500    // 内存历史容量(约 660 KB)
#define MAX_TEXT_W        512    // 单条正文上限
#define MAX_NAME_W        64
#define MAX_HISTORY_LIMIT 500    // 可回看条数上限(排版数组按它分配)
```

消息**从不删除**，只是"显示 / 不显示"的区别 —— 这样展开看历史时旧消息不会消失。

**三个上限的分工**（这也是本项目的"能回看多少"答案）：

| 上限 | 值 | 作用 |
|---|---|---|
| `MAX_MSGS` | 500 | 内存里保存多少条；超出覆盖最旧的 |
| **`MaxHistory`（配置）** | 100（10~500） | **每帧最多拿多少条去排版 ← 实际决定"能往回翻多远"** |
| `MAX_LPP` | 8 | 单条消息最多折成几行，超出部分截断 |

**"名字: 内容"的拼接在 `MsgPush` 入队时就完成**（存在 `ChatMsg::display`），
绘制时直接取用。这样每条消息只存一份文本，绘制路径上不需要大缓冲 ——
历史条数才能从 100 放大到 500 而内存只增加几十 KB
（早期版本为此需要一个 `g_Compose[100][580]` ≈ 580 KB 的缓冲）。

### 4.1.1 超长消息的处理

`CopyW()` 在截断时会**补上 ASCII 的 `...`**，让"这条不完整"一眼可见：

```cpp
if (src[i] && cap >= 4) {          // 源串还有剩余 -> 被截断了
    if (i > cap - 4) i = cap - 4;
    dst[i++] = L'.'; dst[i++] = L'.'; dst[i++] = L'.';
}
```

用 ASCII 的三个点而不是 U+2026 `…`，是因为引擎的 `BitFont` 未必收录那个码位，
显示成方块反而更糟。

### 4.2 行号与显示区间

- `g_Picked[0]` 是**最新**的一条；行号从最旧 0 到最新 `totalAll-1`
- 每条消息的起始行号 = `totalAll` 往回减累加（**最新拿到最大行号**）
- 显示区间 `[rowBegin, rowEnd)`，`skip` 表示"从最新往回跳多少行"

**新消息在下**：`loc.Y = (globalRow - rowBegin) * lineH`。

### 4.3 展开 / 折叠

| 状态 | 行数上限 | 超时过滤 |
|---|---|---|
| 折叠态 | `MaxLines`（默认 8） | ✅ 按超时（≈原版"显示一段时间后消失"） |
| 展开态 | `ExpandLines`（默认 30） | ❌ **忽略超时**，显示完整历史 |

### 4.4 冻结自动滚动

正在看历史（`skip > 0`）时，新消息到达会改变 `totalAll`、把 `skip` 指向别的内容。
做法是把**新增行数补偿进 `g_Scroll`**，使显示区间保持不变；看最新时
（`skip == 0`）不补偿，新消息自然滚动出现。

### 4.5 状态夹紧必须写回自身（重要教训）

`g_Scroll` 是带边界的累加量。**夹紧后必须把值写回状态本身**：

```cpp
if (skip > totalAll - limit) skip = totalAll - limit;
if (g_Expanded) g_Scroll = skip;      // ★ 落回状态, 丢弃积压
```

否则"按过头"的量会静默积压（上限 50 时连按 10 次 PageUp 会让 `g_Scroll`
变成 80，之后要按 4 次 PageDown 才动）。

---

## 5. 交互

| 操作 | 效果 |
|---|---|
| `Ctrl + M` / **左键点框内** | 折叠 ⇄ 展开 |
| **展开时点框外** | 收起 |
| **滚轮上滚**（悬停框上） | 往历史翻（**自动展开**） |
| **滚轮下滚** | 往新消息方向，到底即停 |
| `↑` / `↓` | 滚动一行 |
| `PageUp` / `PageDown` | 翻页（步长为 `ScrollStep` 行，默认 5） |
| `Home` / `End` | 跳到最旧的一屏 / 回到最新 |

按键用 `GetAsyncKeyState` 轮询（上升沿去重），鼠标坐标用 `WWMouseClass::Instance->XY1`。

---

## 6. 取色规则

| 消息类型 | 颜色方案 |
|---|---|
| **玩家聊天**（`name` 非空） | **发言者**的 `ColorSchemeIdx`（与原版一致） |
| **系统 / 剧情**（`name` 为空） | **消息自带**的方案（默认，原版行为）；设 `SysUseMyColor=1` 可改成用**当前玩家**的 `HouseClass::ColorSchemeIndex`，让消息框整体呈现"我的界面"观感 |

两类都只决定"用哪个颜色方案"，实际 RGB 由引擎从方案中取。

---

## 7. 配置项

`ChatBox.ini` 放游戏根目录，与 `ChatBox.dll` 同级。缺失时全用默认值。

```ini
[ChatBox]
ChatBox=1              ; 总开关
LogRaw=0               ; 1 = 完整字段(ret/id/cs/style/to/sp/长度)
LogText=1              ; 记录消息文本(0 = 只记长度)
LogUtf8=0              ; 0 = GBK, 1 = UTF-8(带 BOM)

ShowWindow=1           ; 1 = 接管绘制并显示消息框, 0 = 完全不碰画面
MaxLines=8             ; 折叠态最多显示多少【行】
ExpandLines=15         ; 展开态最多显示多少【行】
ScrollStep=5           ; 翻页/滚轮一次的步长(行)
BoardOpacity=45        ; 底板不透明度 0~100
PosX=8                 ; 窗口左上角
PosY=8
TopGapLines=1          ; 顶部预留几行空档(给回车输入框让位)
KeepLast=1             ; 始终保留的最新消息条数(即使已超时)
MaxHistory=100         ; 一次最多回看多少条消息(10~500)
MaxWidth=0             ; 消息框最大宽度; 0 = 自动(屏幕宽度一半, 上限 520)
SysUseMyColor=0        ; 1 = 系统消息用当前玩家的颜色
FilterSilent=1         ; 1 = 操作提示不进消息框
ShowLogHint=0          ; (预留)
```

**只在插件加载时读取一次** —— 改动后需重启游戏。

---

## 8. 日志

- 路径：`<游戏目录>\MsgLog\YYYY-MM-DD_HH-MM-SS.log`
- **惰性创建**：只在捕获到第一条消息时才建文件。否则启动器等进程加载了 DLL
  却不产生消息，会留下只有头部的空日志（实测踩到过：一次启动出现两份 log）
- 文本里的换行转义成 `\n`，**保证一条消息一行**，便于 grep
- 编码可配：默认 GBK（记事本直接看），`LogUtf8=1` 改 UTF-8（带 BOM）

**两种详细程度，由 `LogRaw` 决定：**

`LogRaw=0`（默认，给人看的）：
```
[09-20 22:15:03] #12 moonbamboo: test
[09-20 22:15:04] #13 难度： 终结
```

`LogRaw=1`（排查/标定用，含全部字段）：
```
[..] #12 ret=0x0055F0FA id=2 cs=7 style=0x4046 to=1259 sp=0
         nameLen=10 textLen=4 name="moonbamboo" text="test"
[..] draw: lineH=17 (BitFont+0x1C=17)     字体度量
[..] draw: cfg MaxWidth=0 (0 = auto)      宽度配置
[..] ui: expanded (click)                 展开/折叠切换记录
```

| 字段 | 含义 |
|---|---|
| `ret` | 调用者返回地址 —— 定位消息由游戏里哪段代码发出 |
| `id` | 消息 ID（玩家聊天时是发言者槽位） |
| `cs` | 颜色方案索引 |
| `style` | 文本样式标志 |
| `to` | 超时帧数（`-1` = 永不消失） |
| `sp` | `silent` 标志（1 = 操作提示类，见 §2） |

这些字段是分流判据与颜色方案索引，**对日常阅读没有意义**；`draw:` / `ui:`
两类界面诊断行同样是排查专用。它们**统一收进 `LogRaw`、默认不输出**。

**无论 `LogRaw` 取值，始终会记录的**：启动头（版本、配置、日志路径）与
每条消息的时间戳 + 序号 + 正文 —— 也就是日常真正要看的东西。

---

## 9. 地址与偏移表

目标引擎：**gamemd.exe（尤里的复仇 1.001，CRC32 = 0x54CC0A13）**。
全部地址均用 `objdump` 反汇编核对；颜色/鼠标相关偏移另有探针实测。

### 9.1 函数与全局

| 项 | 地址 | 说明 |
|---|---|---|
| `MessageListClass::AddMessage` | `0x5D3BA0` | 钩子 1（6 字节） |
| `MessageListClass::Draw` | `0x5D49A0` | 原版绘制，照常执行 |
| 每帧钩子（读输入） | `0x55D360` | 钩子 2（5 字节）；= Phobos `MainLoop_FrameStep_Begin`，AutoKit 同址 |
| 画消息框 | `0x4F455D` → `0x4F4562` | 钩子 3（5 字节），`return 0` 让原版继续 |
| 抑制原版消息列表 | `0x5D4A94` | 钩子 4（7 字节）→ 返回 `0x5D4A9B` |
| `MessageListClass::Instance` | `0xA8BC60` | |
| `Fancy_Text_Print_Wide`（CS 版） | `0x4A61C0` | **cdecl，8 参数** |
| BitFont 折行 | `0x433F50` | thiscall |
| `DSurface::Composite` / `Temp` | `0x88731C` / `0x887314` | |
| `DSurface::ViewBounds` | `0x886FA0` | |
| `BitFont::Instance` | `0x89C4D0` | `+0x1C` = 字高 |
| `ColorScheme::Array` | `0xB054D0` | `Items@+4`、`Count@+0x10` |
| `CurrentPlayer` | `0xA83D4C` | |
| `CurrentFrame` | `0xA8ED84` | 超时判断 |
| `WWMouseClass::Instance` | `0x887640` | `XY1@+0x1C` |
| 滚轮 WndProc | `0x777998` | 钩子 5（6 字节） |
| 侧栏滚动 | `0x533F50` → 跳过 `0x533FC3` | 钩子 6（5 字节） |
| 点击坐标 | `0x692419` → 跳过 `0x6925FC` | 钩子 7（7 字节） |

### 9.2 结构与偏移

| 项 | 值 | 来源 |
|---|---|---|
| `REGISTERS::_ESP` / `_ECX` | `+0x14` / `+0x20` | YRpp/Syringe.h:115 |
| `ColorScheme::BaseColor` | `0x308` | 探针 + 原版 `lea 0x308(%edi)` 双重确认 |
| `ColorStruct` | 3 字节，`R@+0 G@+1 B@+2` | 探针实测 |
| `HouseClass::ColorSchemeIndex` | `0x16054` | 探针实测（3 个锚点交叉验证） |
| `WWMouseClass::XY1` | `+0x1C` | 字段顺序推算 |
| `DynamicVectorClass::Items` / `Count` | `+0x04` / `+0x10` | 同 AutoKit 实测 |

### 9.3 虚表偏移

```
Surface 虚函数顺序: 0=~Surface 1=CopyFromWhole 2=CopyFromPart 3=CopyFrom
                    4=FillRectEx 5=FillRect 6=Fill 7=FillRectTrans ... 30=GetRect
```

| 用途 | 偏移 | 验证 |
|---|---|---|
| `FillRect` | `+0x14` | objdump 实测 `0x623AA8: call *0x14(%edx)` |
| `FillRectTrans` | `+0x1C` | 顺序推出；`0x4BB830` 结尾 `ret $0xC` 吻合 |
| `GetRect` | `+0x78` | 原版 `0x5D4A65: call *0x78(%eax)` |

### 9.4 探针

| 文件 | 用途 |
|---|---|
| `probe/probe_color.cpp` | `ColorScheme` 布局（BaseColor 等） |
| `probe/probe_house.cpp` | `HouseClass::ColorSchemeIndex` |

复跑方式同 AutoKit（`-I override` 必须排在 `-I YRpp` 前）：
```powershell
g++ -std=c++20 -I probe/override -I probe -I <YRpp> -o probe_x.exe probe/probe_x.cpp
```

---

## 10. 构建与部署

```bat
g++ -O2 -shared -fno-exceptions -fno-rtti -o ChatBox.dll ChatBox.cpp ^
    -nostdlib -lkernel32 -luser32 -lgcc ^
    -Wl,--enable-stdcall-fixup -Wl,--no-insert-timestamp -Wl,--entry,_DllMain@12
```

**C++ 受限子集**：不用 STL、不用异常/RTTI、**不用全局对象**（全局构造需要 CRT
启动代码）。目前只用函数指针与 POD 结构 —— 交互是自己做输入处理，没有继承
引擎的 `GadgetClass`（那需要 C++ 对象构造 / 虚表 / `GameAllocator` / 生命周期，
而消息框的交互需求很窄，风险与收益不成比例）。

- 必须 **32 位** mingw（w64devkit-i686-1.23.0，GCC 14.1.0）
- **工具链必须在纯 ASCII 路径**下（中文路径会让 32 位 gcc 找不到库）
- `-Wl,--no-insert-timestamp` 让产物可复现
- 链接 `user32`（`GetAsyncKeyState`）

钩子声明 `ChatBox.dll.inj`（**必须纯 ASCII**）：

```
0x5D3BA0 = ChatBox_AddMessageHook, 6
0x55D360 = ChatBox_FrameHook, 5
0x4F455D = ChatBox_DrawCallHook, 5
0x5D4A94 = ChatBox_MessageDrawHook, 7
0x777998 = ChatBox_ScrollWheelHook, 6
0x533F50 = ChatBox_ScrollSidebarHook, 5
0x692419 = ChatBox_ClickCoordsHook, 7
```

部署到游戏根目录：`ChatBox.dll` + `ChatBox.dll.inj` + `ChatBox.ini`（可选）。
与 NoCopyProtect(7) + AutoLoad(1) + AutoKit(1) 共存时，
Syringe 应显示 **`Done (17 hooks added)`**。

验证：`test/test_load.c` 编译成 32 位程序跑一次（正常输出 `failures: 0`）。

---

## 11. 已知限制与未完成项

| 项 | 状态 |
|---|---|
| 展开 / 滚动 / 鼠标交互 | ✅ 已完成 |
| 滚动条 | ✅ 已完成（指示性；**不支持拖拽**） |
| 折行 | ✅ 已完成 |
| 按来源着色（玩家色 / 系统用我的色） | ✅ 已完成 |
| 向上翻页冻结自动滚动 | ✅ 已完成 |
| 透明度 / 位置 / 宽度 / 行数可配 | ✅ 已完成 |
| **滚动条拖拽** | ❌ 未做（需要选区/拖拽状态机） |
| **时间戳** | ❌ 不打算做（用户决定：日志里有时间戳就够） |
| **未读提示** | ❌ 不打算做（用户评估后放弃） |
| **复制文本** | ❌ 不打算做（用户评估后放弃） |
| **存档内保存** | ❌ 已由"实时写日志"替代 |
| **单条消息超长** | ✅ 超过 512 字符会截断，末尾补 `...` 以示区分 |
| **内存历史上限** | 环形缓冲 500 条；可回看条数由 `MaxHistory` 控制（默认 100，上限 500） |

---

## 12. 项目结构

```
ChatBox/
  src/
    ChatBox.cpp       主源码（捕获 + 绘制 + 折行 + 交互 + 日志）
    offsets.h         全部地址与偏移（含实测依据）
    ChatBox.ini       配置模板
    build.bat         编译脚本（含中文路径 staging）
  probe/
    probe_color.cpp   ColorScheme 布局探针
    probe_house.cpp   HouseClass 颜色字段探针
  test/
    test_load.c       32 位加载验证
  logs/
    MsgLog/           各次实测的原始日志
    MentalOmegaDebugLog/  崩溃现场快照（except.txt / syringe.log）
  ChatBox.dll         插件本体
  ChatBox.dll.inj     钩子声明
  ChatBox.ini         配置
  技术方案.md         本文档
  开发记录.md         实施过程与踩坑
```
